# Extract Structured {language} Content

## Source Report ({language})
{source_report}

## Required JSON Schema
```json
{ui_json_schema}
```

## Task
Extract the report content into a structured JSON object that conforms to the schema above. Output only the JSON object.